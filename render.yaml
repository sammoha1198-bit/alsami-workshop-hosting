services:
  # ---------------- BACKEND ----------------
  - type: web
    name: alsami-backend
    env: python
    plan: free
    buildCommand: pip install --upgrade pip && pip install -r backend/requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: CORS_ORIGINS
        value: "*"

  # ---------------- FRONTEND ----------------
  - type: static
    name: alsami-frontend
    buildCommand: ""
    publishDir: frontend
    autoDeploy: true
